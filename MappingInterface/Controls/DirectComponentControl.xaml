<UserControl x:Class="MappingFramework.MappingInterface.Controls.DirectComponentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MappingFramework.MappingInterface.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="64" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <Label x:Name="LabelComponent" Foreground="ForestGreen" FontWeight="Bold" Grid.Row="0" />
        <StackPanel x:Name="StackPanelComponent" Grid.Row="1">

        </StackPanel>
    </Grid>
</UserControl>
